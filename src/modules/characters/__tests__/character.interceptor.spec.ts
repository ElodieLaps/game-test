import { CharacterInterceptor } from './character.interceptor';

describe('CharacterInterceptor', () => {
  it('should be defined', () => {
    expect(new CharacterInterceptor()).toBeDefined();
  });
});
